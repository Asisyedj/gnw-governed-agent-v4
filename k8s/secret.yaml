# DO NOT commit real values.
# Populate via: kubectl create secret generic gnw-secrets --from-env-file=.env -n gnw
# Or use External Secrets Operator / Vault.
apiVersion: v1
kind: Secret
metadata:
  name: gnw-secrets
  namespace: gnw
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_WITH_REAL_VALUE"
  COOKIE_SECRET: "REPLACE_WITH_REAL_VALUE"
